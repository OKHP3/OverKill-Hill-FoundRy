# GitHub Markdown Rendering Fixture

This fixture is intentionally small and exercises the Markdown constructs used by the export:

| Signal | Meaning |
| --- | --- |
| **Ready** | `yes` |

- Parent item
  - Nested item

[Read the docs](https://example.com/docs)

```ts
const answer = "safe";
```

Before raw HTML <span data-testid="raw-html-boundary">boundary</span> after raw HTML.