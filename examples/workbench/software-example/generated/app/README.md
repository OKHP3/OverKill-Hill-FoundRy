# Contract inspector starter

Open `index.html` in a browser. Enter JSON and choose **Inspect contract** to see whether it parses and which top-level fields and value types it contains. This useful starter behavior does not implement the bespoke JSON Contract Inspector capability.
