# AGENTS.md — JSON Contract Inspector

This package is governed by `OKHP3/OverKill-Hill-FoundRy`. Keep its audience within OverKill Hill, preserve evidence labels, and do not publish or claim behavioral validation without a separate recorded decision.

## Constraints

Dependency-free, local-only, never execute input, and treat names as data.

## Instructions

Parse the input and report its top-level shape; return a readable parse error for invalid JSON.
