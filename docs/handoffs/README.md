# FoundRy assignment index

Use this index to find existing work before assigning another agent. The current
task coordinator maintains links here; each linked issue, PR, or Markdown record
owns the detailed status and evidence. Keep this index limited to this repository.
Do not infer ownership of sibling repositories from the universe context.

## Existing assignment reported by the owner — 2026-09-07

| Task ID | Scope | Reported executor | Record / next action |
| --- | --- | --- | --- |
| foundry-replit-sync | Reconcile this FoundRy Repl with origin/main and verify its runtime | Existing Replit session; identity and checkpoint not yet received | Receiver creates `2026-09-07-foundry-replit-sync-replit.md` here using the [handoff fields](../agent-collaboration.md#compact-task-and-handoff-record), or links its existing issue/PR in this row |

This row preserves the owner's report of work already underway. It is not a new
dispatch, acknowledgement, or verified live status. The integration owner is not
yet recorded. Before assigning overlapping work, obtain the existing Replit
session's checkpoint through its current conversation or an owner-relayed handoff.
Do not create a second synchronization task because the checkpoint is missing.

The receiver records its identity, UTC acknowledgement, base SHA, file scope,
current branch and working-tree state, completed checks, blockers, and next action.
The coordinator and receiver agree the integration owner before any merge. If the
existing task has already finished, record its completion evidence instead of
restarting it. Update this row to the resulting record and retain dated handoff
history when ownership changes.

For later tasks, add only the task ID, short scope, owner, and canonical record
link here. Check both this index and open GitHub work before creating a new record.
Archive completed entries within this file with a date and result link; preserve
their detailed evidence.
