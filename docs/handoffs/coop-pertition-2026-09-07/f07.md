# F07 — Storage recovery regression

Added `artifacts/mockup-sandbox/src/lib/coop-storage.test.ts` with isolated regression coverage for:

- unavailable browser storage and in-memory retry recovery;
- quota failures that preserve the latest accepted edit;
- exact preservation of malformed capability data in a recovery key;
- separation between the capability workspace key and `cgpt-workspace`.

Each scenario restores the prior `localStorage` property descriptor. The tests use the existing storage module without changing its implementation or dependencies.

Validation is recorded after the focused test run and repository governance check.
