# F19 handoff report

Status: complete candidate; pending coordinator integration.

Scope: `artifacts/mockup-sandbox/src/lib/coop-agent-handoff.ts`, `coop-agent-handoff.test.ts`, and this report only. Base acknowledged: `29d3e1abbb833034c083a82922bbe23a8191eb2f`. Branch: `coop/f19`.

The standalone generator creates deterministic plain-text handoff payloads from a `CapabilityProject` and explicit base SHA, allowed files, acceptance criteria, and budget. Every project field is rendered as quoted source data using dynamically sized fences, while authorization remains an explicit caller-controlled statement. The payload includes bounded evidence-return fields and does not claim dispatch, execution, commit, publication, or permission authority.

Validation run:

- `node --experimental-strip-types --test artifacts/mockup-sandbox/src/lib/coop-agent-handoff.test.ts` — pass.
- `pnpm exec vitest run artifacts/mockup-sandbox/src/lib/coop-agent-handoff.test.ts` — not run: checkout has no installed `vitest` dependency.
- `pnpm --dir artifacts/mockup-sandbox typecheck` — not run: checkout has no installed `tsc` dependency.
- `python3 scripts/governance-check.py` — pass.

The task stayed within the assigned file scope and made no UI, store, core, dependency, dispatch, publish, or shared-checkout changes. The acceptance metadata requires a base SHA, nonempty allowed-file list, acceptance text, and budget; missing values are rejected. The test covers stable output, multiline and fence-containing text, authorization boundaries, evidence-return wording, and missing metadata. No repair attempts were needed (0/2).

Local commit: `28c5672445b1e40d71c15001aef450d5ced0a834` (this report was included in that commit).
