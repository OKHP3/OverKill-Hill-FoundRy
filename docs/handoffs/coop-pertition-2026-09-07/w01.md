# W01 handoff — FoundRy feature-page contract tests

## Scope

Added `tests/test-foundry-feature-contract.py`, a standard-library `unittest`
contract suite for the FoundRy authoring source and rendered feature page.
The checks cover the canonical workbench URL, the separate GPT-studio scope,
local-only persistence and recovery wording, named target kinds, and language
that distinguishes a specification from executed or hosted behavior.

The separate runtime application is not present in this website checkout, so
the suite does not claim to test browser execution, storage APIs, or deployment.
Existing website A03-A21/T01-T06/W13 ownership was not changed.

## Future parity boundary

Two checks are explicitly opt-in with `FOUNDRY_EXPECTED_PARITY=1`:

- named project slots;
- backup/project import or restore controls.

They describe desired runtime parity and are skipped by default. Running them
against the current page is expected to expose the current gaps; that result is
not evidence that future behavior has shipped.

## Validation

- `python3 tests/test-foundry-feature-contract.py`: pass (6 passed, 2 skipped).
- `FOUNDRY_EXPECTED_PARITY=1 python3 tests/test-foundry-feature-contract.py`:
  expected diagnostic failures for the two future checks; current page remains
  honest about named slots being on the roadmap and export being manual.
- Broader site gates, browser QA, build generation, and deployment checks were
  not run because this task owns only the focused test and handoff paths.

## Candidate

Branch: `coop/w01`

The local candidate commit is reported to the coordinator after the focused
checks complete. No push, merge, deploy, shared-checkout write, or dependency
change was performed.
