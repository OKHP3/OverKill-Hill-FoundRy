# F03 handoff — workflow package example

## Scope acknowledgement

I acknowledge the F03 scope from the coordinator README: I own only `examples/workbench/workflow-example/` and this handoff file, on branch `coop/f03` in the isolated F03 worktree. The base is the dispatched FoundRy baseline `29d3e1abbb833034c083a82922bbe23a8191eb2f` as checked out here. No existing source, dependencies, owner checkout, push, merge, PR, or deployment was changed.

## Delivered

Built a public-safe local release evidence checklist workflow project and version 1 importable backup. The package was generated through the existing `buildCapabilityFiles` generator. Added a worked synthetic pass/fail walkthrough with explicit human actions and a clear boundary around structural checks versus workflow-engine execution.

## Validation

- `node --experimental-strip-types` invoked the real generator to produce the package files and `createCapabilityBackup` to produce `backup.json`.
- `node --experimental-strip-types` backup round-trip check: pass (the generated backup parses through `parseCapabilityBackup`; exact command and result are recorded in the coordinator result).
- `python3 scripts/governance-check.py`: not run; this task owns an example and handoff only, and the coordinator should run the repository-wide gate after integration.

## Limits

The walkthrough is synthetic authored material. No scheduler, workflow engine, model behavior, external release, or publication authorization was executed or established. The generated package remains a structural draft.

## Commit

Recorded after validation: see the coordinator message for the exact local commit SHA.
