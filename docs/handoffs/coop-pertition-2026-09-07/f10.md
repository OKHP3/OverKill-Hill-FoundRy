# F10 handoff — narrow viewport acceptance

## Scope and base

- Task: F10, narrow viewport acceptance.
- Branch: `coop/f10`.
- Worktree: `.cache/coop-pertition/worktrees/f10`.
- Baseline observed from the dispatch contract: `29d3e1abbb833034c083a82922bbe23a8191eb2f`.
- Owned files: `artifacts/custom-gpt-creator/tests/coop-responsive.e2e.ts` and this handoff.
- No application, CSS, runtime, dependency, hosting, shared-checkout, push, merge, or deploy changes were made.

## Candidate

`coop-responsive.e2e.ts` adds four bounded Playwright cases covering 320×800 and 390×844 viewports in light and dark themes. Each case enters a long project name, populates the contract/build/validation controls, verifies the package-stage controls are visible, checks the generated ZIP filename, and captures a viewport screenshot when it reaches that point.

## Focused evidence

Command:

```text
PORT=21010 pnpm --filter @workspace/custom-gpt-creator test:e2e tests/coop-responsive.e2e.ts
```

Result: 4 failed, 0 passed. The failures are intentionally retained as the minimal reproduction; no assertion was weakened and no source fix was attempted under the F10 scope.

Reproduction: at each viewport/theme combination, open the capability workbench, enter `A capability project name that remains readable at narrow widths — 2026` in `Capability name`, fill the owner/purpose/audience fields, then assert `document.documentElement.scrollWidth <= window.innerWidth`. The assertion remains false after the long-name content is entered at both 320px and 390px, in both light and dark themes.

Playwright produced failure traces and error contexts under `.cache/custom-gpt-creator-test-results/` outside the owned source paths. The failure is a current responsive overflow defect; likely CSS/runtime ownership remains with the application owners.

## Governance

Command:

```text
python3 scripts/governance-check.py
```

Result: passed. Manifest, registry, child scaffold/sync, public graduation dry run, and release-record consistency checks all passed.

## Limits

- Browser plugin was unavailable in this session; regular Playwright was used with the repository's existing configuration.
- The focused run stops at the overflow assertion, so package-stage ZIP filename and later control assertions are present in the candidate but not reached until the responsive defect is fixed.
- No CSS/runtime repair was authorized for this task. A follow-up owner should inspect the rendered overflow using the retained traces/screenshots and rerun this file after repair.
