# F04 handoff — software starter example

## Acknowledgement

- Base: `29d3e1abbb833034c083a82922bbe23a8191eb2f` as recorded in the dispatch. The assigned checkout is branch `coop/f04` under `.cache/coop-pertition/worktrees/f04`.
- Scope: `examples/workbench/software-example/` and this handoff file only.
- Contract accepted: no existing-source edits, dependency or hosting changes, owner-checkout writes, push, merge, deploy, or bespoke business-tool claim.

## Delivered

The example uses the existing `buildCapabilityFiles`, `createCapabilityBackup`, and `newProject("software")` generator APIs. The generated package includes an importable version 1 backup and generated `app/index.html`. `smoke-check.mjs` parses the backup and executes the generated inline starter JavaScript in Node's VM runtime with a minimal DOM harness.

Cases covered:

- valid object with number, array, and null fields;
- malformed JSON returning `validJson: false`;
- hostile text treated as data, with no extra script tag created.

## Validation

Commands run from the repository root:

```text
node --experimental-strip-types examples/workbench/software-example/generate.mjs
node examples/workbench/software-example/smoke-check.mjs
```

Result: both passed. The checker is repeatable and uses the generated source after regeneration.

The repository governance check was also run:

```text
python3 scripts/governance-check.py
```

Result: passed — manifest, registry, scaffold sync, public graduation dry run, and release-record consistency checks all passed.

## Limits

The smoke check is an automated JavaScript-runtime check with a minimal DOM harness. It does not establish layout, accessibility, or cross-browser behavior. A manual browser check is documented in the example README. The generated starter reports JSON shape only; it does not implement or validate a bespoke business contract, and the package remains an experimental example.
