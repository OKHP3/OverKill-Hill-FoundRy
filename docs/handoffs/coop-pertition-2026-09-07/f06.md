# F06 — Backup byte-boundary regression

Added `artifacts/mockup-sandbox/src/lib/coop-backup-bounds.test.ts` against the existing capability backup parser/exporter.

Coverage is table-driven at the UTF-8 field limit (40,000 bytes), workspace project ceiling (20 projects), and import limit (2,000,000 bytes). It also records the current contract for envelope extensions, rejects an unknown schema version, and verifies that parsed project arrays and values are isolated between parses. Existing tests already cover malformed JSON, unknown schema/version, foreign audience, duplicate IDs, unknown project fields, oversized input, and a basic round-trip reference check; those cases were not duplicated.

Validation and commit evidence are recorded in the worker handoff message. No parser, source, dependency, shared checkout, push, merge, or deployment changes were made.
