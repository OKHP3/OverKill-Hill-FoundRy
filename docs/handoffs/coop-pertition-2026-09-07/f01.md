# F01 handoff — prompt package example

## Scope and base

- Branch: `coop/f01`
- Base observed: `29d3e1abbb833034c083a82922bbe23a8191eb2f`
- Owned paths: `examples/workbench/prompt-example/` and this handoff file only.
- No existing source, dependencies, hosting, owner checkout, push, merge, or deployment changed.

## Result

Added a public-safe action-item extraction prompt project, representative authored expectation cases, an importable backup, and generated package files. The fixture script calls the existing `buildCapabilityFiles`, `createCapabilityBackup`, and `parseCapabilityBackup` implementation.

## Validation

- `node --strip-types examples/workbench/prompt-example/generate.ts` — passed; generated 7 files and verified backup round trip.
- Generator output is the direct result of the existing workbench generator; the script writes every returned path and verifies the backup round trip.

The cases are authored expectations only. No model was invoked, so behavioral correctness is not established.

## Limits

The generated package is an experimental example and is not a released feature, evaluated prompt, or publication authorization.
