# F17 handoff — backup migration design

## Scope and base

Branch: `coop/f17`  
Worktree: `.cache/coop-pertition/worktrees/f17`  
Base requested by dispatch: `29d3e1abbb833034c083a82922bbe23a8191eb2f`  
Owned files: `docs/workbench-backup-migration-design.md` and this handoff.

## Result

Added an implementation-ready, proposed migration contract around the observed
version-1 backup envelope. It cites the parser, validator, UI confirmation path,
and existing tests; specifies exact field names and limits; requires original
byte preservation, dry-run reporting, unknown-version failure, write/read-back
verification, and rollback; and provides a recovery fixture matrix. No prior
supported schema is asserted.

## Validation and limits

Governance and scoped diff checks were run before handoff. The document is a
design artifact only: it does not add an adapter, alter the parser, implement
rollback, or establish production behavior.
