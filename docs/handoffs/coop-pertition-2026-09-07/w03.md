# W03 handoff, Found‑Rᵧ metadata parity audit

Status: **complete candidate, proposed not applied**.

## Scope acknowledged

W03 owns the metadata parity audit at
`assets/docs/foundry-metadata-parity-2026-09-07.md` and this handoff record.
The audit compares the website's Found‑Rᵧ title, description, social fields,
iframe title, and image alt text to the supplied app-source boundary. It does
not edit `site-src/`, `projects/`, app source, configuration, CSS, generated
HTML, dependencies, hosting, or publication state. No A06 general claim queue
was used.

## Evidence and limitation

Website source and generated metadata were read from:

- `site-src/pages/projects/found-ry/index.main.html`
- `site-src/pages.json`
- `projects/found-ry/index.html`

The supplied read-only comparison sources were the actual capability workbench
(`artifacts/mockup-sandbox/src/pages/capability-workbench.tsx` and
`artifacts/mockup-sandbox/src/lib/capability-workbench.ts`) plus the separate
Custom GPT studio head (`artifacts/custom-gpt-creator/index.html`). The audit
records the four released capability kinds and keeps all replacement copy
explicitly proposed.

## Files changed

- `assets/docs/foundry-metadata-parity-2026-09-07.md`
- `docs/handoffs/coop-pertition-2026-09-07/w03.md`

## Validation

- Confirmed branch is `coop/w03`.
- Confirmed working tree was clean before drafting.
- Checked exact source locations with `nl -ba` and `rg`.
- Attempted `python3 scripts/governance-check.py` after drafting; it could not
  run because this worktree does not contain `scripts/governance-check.py`
  (Python exited 2 before executing a check).

The audit is documentation-only, so no browser, build, dependency, network,
or publication check is appropriate. The unavailable governance entry point is
recorded as a validation limitation, not a pass. It does not change the
proposed/not-applied status.

## Integration note

Commit locally on `coop/w03`. The coordinator must review the evidence boundary
and obtain the released workbench's real metadata source before treating any
copy pair as a confirmed parity fix. No push, merge, deploy, or shared-checkout
write was performed by W03.
