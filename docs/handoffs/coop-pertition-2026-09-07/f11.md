# F11 — Studio coexistence acceptance

## Scope and base

- Scope acknowledged: `artifacts/custom-gpt-creator/tests/coop-coexistence.e2e.ts` and this handoff only.
- Worktree: `.cache/coop-pertition/worktrees/f11` on `coop/f11`.
- Dispatch base observed: `29d3e1abbb833034c083a82922bbe23a8191eb2f`.
- No source, dependency, lockfile, hosting, deployment, or owner-checkout changes.

## Acceptance implemented

The Playwright case seeds the capability workbench and Custom GPT studio namespaces independently, preserves an unrelated browser key, follows the workbench → studio hash route with browser Back/Forward, reloads both surfaces, and asserts all three stored values remain unchanged. It does not clear browser storage or duplicate the existing broad workbench journey coverage.

## Validation

- Command: `PORT=21011 pnpm exec playwright test tests/coop-coexistence.e2e.ts`
- Result: passed — 1 test passed (1.9s).
- Governance command: `python3 scripts/governance-check.py`
- Result: passed — all governance checks passed.

## Limits

This is browser acceptance evidence for the current local artifact. It does not establish production deployment, cross-browser coverage, or persistence beyond the browser local-storage implementation.
