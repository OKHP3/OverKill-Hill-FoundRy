auto-install-peers=false
strict-peer-dependencies=false
# Keep local Replit workflows from recursively bootstrapping a second pnpm
# process when the workspace's canonical packageManager pin is unavailable.
manage-package-manager-versions=false
